// In the asset pack’s build.gradle file:
apply plugin: 'com.android.asset-pack'

assetPack {
    packName = "ff2" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "fast-follow"
    }
}
